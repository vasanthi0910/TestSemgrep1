rules:
- id: my_pattern_id
  pattern: |
      public $RETURN $METHOD(...){
        ...
        make_transaction($T);
        ...
      }
  message: Semgrep found a match
  languages: [java]
  severity: Error
